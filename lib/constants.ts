/** How long the round-result screen stays up before auto-advancing to the
 * next question. Shared between HostRoom (which actually triggers the
 * advance) and PlayRoom (which only displays a matching countdown). */
export const NEXT_QUESTION_DELAY_SECONDS = 8;
